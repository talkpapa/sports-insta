# 지금까지의 상태 (2026-08-29)

새 세션에서 이 파이프라인을 이어받을 때 먼저 읽는 문서.
코드에 남긴 주석이 "왜 그렇게 짰는가"를 담고 있으므로, 고치기 전에 해당 파일의
주석을 먼저 볼 것.

## 무엇을 하는 계정인가

`@sportsnewsstation` — **축구 이적시장 전문** 릴스를 하루 3편, 사람 손 없이 올린다.
프로필 이름은 `Football Transfer News`.

카드 세 장의 역할이 서로 다르다. 이걸 흐리면 계정의 이유가 없어진다.

| 카드 | 역할 | 초 |
|---|---|---|
| 1 | 눈길을 잡는 큰 제목 | 5 |
| 2 | 배경을 깔고 던지는 **왜/어떻게** 질문 | 5 |
| 3 | 그 질문에 대한 **자세한 답** (35~55단어) | 8 |

"Club A signed Player B" 는 구단 계정이 더 빨리 알린다. 우리가 줄 수 있는 것은
**왜 그런가**뿐이다. 설명할 재료가 없는 소재는 건너뛴다 — 지어내는 것보다 낫다.

## 하루의 흐름

| 시각(KST) | 무엇 | 어디서 |
|---|---|---|
| 06:00 예정 (실제 ~09:30) | 빌드 — 뉴스→원고→사진→카드→영상→Pages | GitHub Actions |
| 11:30 / 15:30 / 19:30 예정 (실제 +1.5~2시간) | 게시 한 편씩 | GitHub Actions |
| 01:00 예정 | 대조 — 기록과 계정이 맞는지 | GitHub Actions |
| 11~22시 매시간 | 감시자 — 빠진 것이 있으면 위 워크플로를 깨움 | **이 PC** |

**GitHub 예약 실행은 약속이 아니다.** 8월 27일에는 하루 종일 아무것도 안 돌았고,
28일에는 세 자리 중 하나가 통째로 건너뛰어졌다. 워크플로는 전부 active 였고 수동
실행은 즉시 됐다 — 설정 문제가 아니라 GitHub 쪽 사정이라 cron 을 손봐도 안 고쳐진다.
그래서 PC 의 작업 스케줄러(`TIM-SportsWatchdog`)가 매시간 대신 깨운다.

감시자는 판단을 최소로만 한다 — "오늘 몫이 남았는가"만 보고 깨운다. 하루 한도,
게시 간격, 게시 전 검사는 전부 서버가 그대로 하므로 여러 번 깨워도 넘치지 않는다.

## 상태 보기

**https://talkpapa.github.io/sports-insta/** — 폰에서 여는 상태판. 최근 7일을
하루씩, 나간 것은 시각과 링크, 안 나간 것은 대기로 보여준다. 토큰이 필요 없다.

```bash
node scripts/ig.js check 7      # 기록과 실제 계정을 대조 (IG_TOKEN 필요)
node scripts/watchdog.js --dry  # 감시자가 무엇을 할지만 보기
node scripts/build.js --dry     # 오늘 무엇을 만들 수 있는지만 보기
```

## 사람 손이 필요한 것 하나

**인스타 토큰은 60일이면 죽는다.** 매주 월요일 `refresh.yml` 이 새 토큰을 로그에
남기지만, GitHub Actions 는 자기 시크릿을 스스로 못 바꾼다. 로그의 토큰을
`Settings → Secrets → IG_TOKEN` 에 붙여넣어야 실제로 연장된다.
안 하면 60일째에 조용히 멈춘다.

## 겪은 사고들 — 같은 실수를 되풀이하지 말 것

사진 필터가 여러 겹인 데는 다 이유가 있다. 하나씩 사고를 겪고 생긴 것이다.

| 무슨 일 | 지금 막는 방법 |
|---|---|
| 누드 회화가 표지로 뽑힘 | 검색어 낱말이 제목에 있어야 함 |
| 1947년 흑백 기록사진 | 제목에 1995년 이전 연도가 있으면 거름 |
| 워드프레스 마스코트 그림 | 그림·벽지·목업 낱말 거름 |
| **파스텔 소묘** (제목·category 로는 못 걸림) | **태그에 art/illustration/painting 이 있으면 거름** |
| 세 편이 같은 배경을 공유 | 최근 30장 기억 + 검색어 하나에서 한 장만 |
| 한 편의 카드 셋이 다 잔디 | 검색어를 결로 묶어 번갈아 씀 |

그 밖에:

- **서버에 키가 안 갔다** — `util.js` 가 읽을 환경변수 이름을 목록으로 두고 있었고
  거기에 `GEMINI_API_KEY` 가 없었다. 로컬에는 `.env` 가 있어 잘 돌아서 하루가
  지나서야 알았다. 목록을 없앴다.
- **캡션 검사가 선수 이름을 오류로 잡았다** — `NaN` 을 대소문자 무시하고 찾다가
  "Fer**nan**des" 에 걸려 멀쩡한 두 편이 하루를 흘려보냈다. 낱말 경계를 본다.
- **자정을 넘겨 한 편이 버려졌다** — 21:30 게시가 2시간 43분 늦어 0시 13분에 돌았고,
  그 시점의 "오늘"은 다음 날이라 그 날짜 큐가 없었다. 시각을 앞당기고, 오늘 것이
  없으면 어제 큐도 보게 했다.
- **같은 이적이 두 편으로 나갔다** — 겹침 비율만 보다가 0.33 으로 통과했는데 하필
  겹친 둘이 `liverpool` 과 `barcola` 였다. 이제 흔한 말과 구단 이름을 걷어낸 뒤
  남은 이름(대개 선수)이 겹치면 같은 이야기로 본다.
- **한글 경로를 .cmd 에 적으면 안 된다** — cmd.exe 가 콘솔 코드페이지로 읽어서
  깨진다. `watchdog.cmd` 는 `%~dp0` 로 자기 위치를 알아낸다.

## 무인 파이프라인의 원칙

**조용한 성공이 조용한 실패보다 나쁘다.** 전부 터진 빌드가 초록불로 끝난 날이
있었다. 이제 모델이 건너뛴 것(정상)과 오류로 터진 것(고장)을 갈라 세고, 한 편도
못 만들었는데 오류가 있었으면 실패로 끝낸다. 밤 대조는 "어제 큐가 아직 남아
있으면" 실패로 본다.

**게시는 API 로 삭제할 수 없다.** 그래서 조금이라도 이상하면 올리지 않고 멈춘다.
한 편 거르는 것이 이상한 영상이 영구히 남는 것보다 낫다.
